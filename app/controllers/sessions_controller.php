<?
$app->get("/login", function() use ($app) {

});

$app->get("/logout", function() use ($app) {
  // $user->logout();
  // $app->redirect("/");
});


$app->post("/login", function() use ($app) {
  
});